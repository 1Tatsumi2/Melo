@model IEnumerable<MusicApp.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_The_Loai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_The_Loai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ma_The_Loai }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ma_The_Loai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ma_The_Loai })
        </td>
    </tr>
}

</table>
